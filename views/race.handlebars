{{!-- page user is brought to after they choose to join a race, or create one. Greets user with their username, and displays the ID of the current race they are in. Form for entering the user's racer choice, and to add a message. --}}

<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{username}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3 class="center">Current race: {{race_name}}</h3>

    <form class="form new-project-form" id="new-race-form">
      <div class="form-group">
        <label for="racer-id">Enter your winning racer #:</label>
        <input class="form-input" type="number" min="1" max="20" id="racer-id" name="racer-id" />
      </div>
      <div class="form-group">
        <label for="participant-message">Add a message:</label>
        <textarea class="form-input" type="text" id="participant-message" name="participant-message"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary" id={{race_id}}>Create</button>
      </div>
    </form>
  </div>

  

<script src="/js/race.js"></script>
